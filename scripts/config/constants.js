var config;
(function (config) {
    // It is an Abstract class created to be used to hold constant variables
    var Constants = /** @class */ (function () {
        function Constants() {
        }
        Constants.canvasHeight = 480;
        Constants.canvasWidth = 640;
        return Constants;
    }());
    config.Constants = Constants;
})(config || (config = {}));
//# sourceMappingURL=constants.js.map